node_modules/
npm-debug.log*
*.log
dist/
sidecar-build/
sidecar-evals/.artifacts/
.omc/
.omx/
.DS_Store
.env
.env.local
.env.*.local
xcuserdata/
*.xcuserstate

# Local secrets (Apple Developer credentials, build env)
secrets/

# Local build artifacts (xcodebuild archive, DMG, logs). Sidecar already ignored above.
build/

# Private strategy/alignment inputs. Do not commit raw Founder/company planning data.
docs/private/
docs/_private/
docs/mandal-art*.md
docs/mandalart*.md
docs/*mandal*.md
docs/october-academy/

# Local/generated assistant memory bootstrap.
docs/SOUL.md

# Daily execution roadmap (private — contains founder time budget and Q3 strategy)
PLAN.md
.gstack/

# Local Claude Code agent skills cache (e.g. PostHog wizard scaffold).
# Reference EXAMPLE.md ships a sample phc_ token; never commit this folder.
.claude/

# Local one-shot wizard reports (PostHog setup, etc.). Internal dashboard IDs
# only — not source. Regenerated by the wizard, no value in vcs.
posthog-setup-report.md
